<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CheesePrismFilmAnnotator"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" x:Class="CheesePrismFilmAnnotator.MainWindow"
        mc:Ignorable="d"
        Title="로그인" Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource SystemControlBackgroundAccentBrush}">
    <Grid>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="아이디" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}" Margin="0,0,8,0"/>
            <TextBox x:Name="txtUsername" Grid.Row="0" Grid.Column="1" Height="28" KeyDown="Input_KeyDown" Style="{DynamicResource FluentTextBoxStyle}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="비밀번호" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}" Margin="0,8,8,0"/>
            <PasswordBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" Height="28" KeyDown="Input_KeyDown" Style="{DynamicResource FluentPasswordBoxStyle}" Margin="0,8,0,0"/>

            <Button x:Name="btnLogin" Grid.Row="2" Content="로그인" Width="100" Height="32" Margin="0,5,0,3" Click="btnLogin_Click" Style="{DynamicResource FluentButtonStyle}" HorizontalAlignment="Left" Grid.Column="1"/>
        </Grid>
    </Grid>
</Window>
